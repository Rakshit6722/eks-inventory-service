PORT=3001

# Option A (recommended): set a full connection string
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# Option B: set individual connection fields
DB_HOST=
DB_PORT=5432
DB_NAME=postgres
DB_USER=
DB_PASSWORD=

# RDS commonly supports/uses TLS. Default in code is true.
DB_SSL=true